# 越山对话ai - 环境变量模板
# 请复制此文件为 .env 并填写实际值

# 管理后台登录密码
ADMIN_PASSWORD=[REDACTED]

# 后台管理二次认证密码（可选）
# 设置后进入模型管理后台需单独验证此密码；未设置则回退到 ADMIN_PASSWORD 并仍要求二次确认。
# 建议与前台访问密码不同，避免普通对话用户越权管理模型/API Key。
# ADMIN_PANEL_PASSWORD=[REDACTED]

# 可信反向代理 IP（可选）
# 仅当服务器位于固定反代/CDN 之后时填写反代的出口 IP，此时才会信任 X-Forwarded-For。
# 留空则始终使用 REMOTE_ADDR 作为用户真实 IP（推荐）。
# TRUSTED_PROXY_IP=10.0.0.1

# OpenAI API Key (如果不在 models.json 中单独配置)
# OPENAI_API_KEY=sk-xxxx...
